{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "my-dotclaude",
  "owner": {
    "name": "William Chastain",
    "url": "https://github.com/CrazyWillBear"
  },
  "plugins": [
    {
      "name": "context",
      "source": "./plugins/context",
      "description": "Context-window management: the four hooks (watchdog, resume, save-handoff, suggest-docs) that drive deliberate, early /clear and /handoff as a session's window fills, plus /handoff and /handoff-plan themselves. Calls into nothing else, so every other plugin can depend on it."
    },
    {
      "name": "personal-tools",
      "source": "./plugins/personal-tools",
      "description": "My personal Claude Code slash commands and subagents."
    },
    {
      "name": "workflow",
      "source": "./plugins/workflow",
      "description": "An autonomous dev loop: /orchestrate is a standing dispatcher that routes work by shape — one explicit unit runs as a subagent chain, an issue graph or PRD gets one real background Claude Code session per issue in its own git worktree, coordinating over the issue thread and reporting back by message — with my-review reviewing every built slice, a deterministic merge fold, and every irreversible GitHub write kept on the main thread."
    }
  ]
}
