// PLACEHOLDER — replaced by the deployed artifact's code at deploy time.
// Must default-export a zero-required-props React component (the claude.ai convention).
export default function Artifact() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-background text-foreground">
      <p className="text-muted-foreground">No artifact loaded.</p>
    </div>
  )
}
